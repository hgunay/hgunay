name: Waka - Readme

on:
  schedule:
    # Runs at 12am UTC
    - cron: '0 0 * * *'

jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: uses: athul/waka-readme@0.1.2
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
